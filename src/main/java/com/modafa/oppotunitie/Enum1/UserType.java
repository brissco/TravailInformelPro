package com.modafa.oppotunitie.Enum1;


public enum UserType {
    WORKER, // Represents users looking for jobs
    EMPLOYER, // Represents users posting job offers
    ADMIN // Represents administrative users with higher level privileges
}